My set of dotfiles (CLI config files) to bring some comfort.


Installation
------------

NOTE: Needs to be at `~/.dotfiles`

```console
$ git clone git@github.com:joonas-fi/dotfiles.git ~/.dotfiles
$ cd ~/.dotfiles/
$ ./install.sh
```
